# Автоматический деплой без ввода пароля

## 🚀 Быстрая настройка (один раз)

### Шаг 1: Настройте SSH ключи

Запустите скрипт настройки (потребуется ввести пароль **ОДИН РАЗ**):

```powershell
.\setup-ssh-keys.ps1
```

Или двойной клик на `setup-ssh-keys.bat`

**Что делает скрипт:**
1. Генерирует SSH ключ (если его нет)
2. Копирует публичный ключ на сервер
3. Настраивает доступ без пароля

После этого пароль больше не потребуется!

---

## 📝 Использование

### Автоматический деплой (без пароля):

```powershell
.\deploy-auto.ps1
```

Или двойной клик на `deploy-auto.bat`

### Быстрое обновление файла (без пароля):

```powershell
.\quick-update.ps1 backend\app\api\viewer.py
```

### Обновить все файлы:

```powershell
.\quick-update.ps1
```

---

## 📋 Доступные скрипты

| Скрипт | Описание | Требует пароль? |
|--------|----------|-----------------|
| `setup-ssh-keys.ps1` | Настройка SSH ключей (один раз) | Да (один раз) |
| `deploy-auto.ps1` | Полный автоматический деплой | Нет |
| `quick-update.ps1` | Быстрое обновление файлов | Нет |
| `deploy-with-password.ps1` | Деплой с вводом пароля | Да (каждый раз) |

---

## ⚠️ Важно

1. **Настройте SSH ключи один раз** - после этого все скрипты будут работать автоматически
2. **Безопасность**: SSH ключи безопаснее чем хранение паролей в файлах
3. **Если SSH ключи не настроены**: Скрипты будут запрашивать пароль каждый раз

---

## 🔧 Ручная настройка SSH ключей

Если скрипт не работает, можно настроить вручную:

1. **Сгенерируйте ключ** (если его нет):
   ```powershell
   ssh-keygen -t rsa -b 4096
   ```

2. **Скопируйте публичный ключ на сервер**:
   ```powershell
   type $env:USERPROFILE\.ssh\id_rsa.pub | ssh root@89.110.111.184 "cat >> ~/.ssh/authorized_keys"
   ```

3. **Проверьте подключение**:
   ```powershell
   ssh root@89.110.111.184 "echo 'SSH key works!'"
   ```

---

## 💡 Рекомендации

- Используйте `deploy-auto.ps1` для полного деплоя
- Используйте `quick-update.ps1` для быстрого обновления одного файла
- После настройки SSH ключей все работает автоматически!
