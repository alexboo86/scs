# Python Deploy Script

## 🚀 Быстрый старт

### 1. Установите зависимости:

```bash
pip install -r requirements-deploy.txt
```

Или напрямую:

```bash
pip install paramiko
```

### 2. Запустите деплой:

```bash
python deploy.py
```

Или двойной клик на `deploy.bat`

---

## ✅ Преимущества Python скрипта

- ✅ **Надежнее** - не зависает на файлах
- ✅ **Быстрее** - эффективная работа с SFTP
- ✅ **Таймауты** - автоматическая обработка зависаний
- ✅ **Фильтрация** - автоматически исключает `__pycache__` и `.pyc` файлы
- ✅ **Прогресс** - показывает сколько файлов скопировано
- ✅ **SSH ключи** - автоматически использует настроенные ключи

---

## 📋 Что делает скрипт

1. Подключается к VPS через SSH (использует SSH ключи)
2. Синхронизирует файлы через SFTP:
   - `backend/app`
   - `frontend/templates`
   - `docker-compose.yml`
3. Исключает ненужные файлы (`__pycache__`, `.pyc`, и т.д.)
4. Перезапускает backend контейнер
5. Показывает статус контейнеров

---

## ⚙️ Настройка

Если нужно изменить IP или пользователя, отредактируйте переменные в начале `deploy.py`:

```python
VPS_IP = "89.110.111.184"
VPS_USER = "root"
PROJECT_DIR = "secure-content-service"
```

---

## 🔧 Требования

- Python 3.7+
- paramiko (устанавливается через `pip install paramiko`)
- Настроенные SSH ключи (или запустите `setup-ssh-keys.ps1`)

---

## 💡 Использование

### Полный деплой:
```bash
python deploy.py
```

### Или через BAT:
```bash
deploy.bat
```

Скрипт автоматически:
- Найдет путь к проекту
- Подключится к серверу
- Скопирует файлы
- Перезапустит контейнер
