# Быстрые команды для деплоя

## 🚀 После настройки алиасов

### Настройка (один раз):

```powershell
.\setup-powershell-aliases.ps1
```

Перезапустите PowerShell или выполните:
```powershell
. $PROFILE
```

### Использование:

```powershell
# Обновить один файл
update backend\app\api\viewer.py

# Перезапустить backend
restart

# Полный деплой
deploy

# Подключиться к VPS
vps

# Показать логи
logs
```

---

## 📝 Через BAT файлы (двойной клик)

### Обновить файл:
1. Правой кнопкой на файл → "Копировать путь"
2. Двойной клик на `update-file.bat`
3. Вставьте путь к файлу

### Или через командную строку:
```cmd
update-file.bat backend\app\api\viewer.py
```

### Перезапустить backend:
Двойной клик на `restart-backend.bat`

### Полный деплой:
Двойной клик на `deploy-all.bat`

---

## ⚡ Самые частые команды

### Обновить viewer.py и перезапустить:
```powershell
update backend\app\api\viewer.py
restart
```

### Обновить viewer.html и перезапустить:
```powershell
update frontend\templates\viewer.html
restart
```

### Полный деплой после изменений:
```powershell
deploy
```

---

## 🎯 Рекомендации

- **Для одного файла:** `update файл` + `restart`
- **Для нескольких файлов:** `deploy`
- **Для проверки логов:** `logs`
