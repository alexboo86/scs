# 🚀 Быстрое обновление на сервере

## Самый быстрый способ (рекомендуется)

### 1. Обновить один файл:
```powershell
.\quick-update.ps1 backend\app\api\viewer.py
```

### 2. Обновить все файлы проекта:
```powershell
.\quick-update.ps1
```

### 3. Обновить без перезапуска (если используется hot-reload):
```powershell
.\quick-update.ps1 backend\app\api\viewer.py -NoRestart
```

---

## Через BAT файлы (двойной клик)

### Обновить один файл:
```cmd
quick-update.bat backend\app\api\viewer.py
```

### Обновить все:
```cmd
quick-update.bat
```

---

## Настройка алиасов для еще более быстрого доступа

### 1. Запустите один раз:
```powershell
.\setup-powershell-aliases.ps1
```

### 2. Перезагрузите PowerShell или выполните:
```powershell
. $PROFILE
```

### 3. Теперь можно использовать короткие команды:
```powershell
# Обновить файл
update backend\app\api\viewer.py

# Перезапустить backend
restart

# Показать логи
logs

# Подключиться к VPS
vps
```

---

## ⚡ Самые частые сценарии

### Изменили Python файл:
```powershell
.\quick-update.ps1 backend\app\api\viewer.py
```

### Изменили HTML шаблон:
```powershell
.\quick-update.ps1 frontend\templates\viewer.html
```

### Изменили несколько файлов:
```powershell
.\quick-update.ps1
```

### Только перезапустить (без обновления файлов):
```powershell
.\restart-backend.bat
```

---

## 📝 Примечания

1. **Hot-reload**: Если в docker-compose.yml настроен volume mount (`./backend/app:/app/app`), то изменения могут применяться автоматически без перезапуска. В этом случае используйте `-NoRestart`.

2. **Проверка логов**: После обновления проверьте логи:
   ```powershell
   ssh root@89.110.111.184 "cd ~/projects/secure-content-service && docker-compose logs -f backend"
   ```

3. **Полный деплой**: Если нужно пересобрать контейнер (например, изменили requirements.txt или Dockerfile):
   ```powershell
   .\deploy.ps1
   ```

---

## 🔧 Настройка путей

Если ваш проект находится не в стандартной директории, отредактируйте:
- `quick-update.ps1` - автоматически определяет путь от места запуска
- `deploy-single-file.ps1` - автоматически определяет путь от места запуска
- `setup-powershell-aliases.ps1` - укажите правильный путь в функции

---

## ⚠️ Важно

- Скрипты используют пароль из файла. Для безопасности лучше настроить SSH ключи.
- Файлы копируются напрямую в volume mount директорию, поэтому изменения применяются сразу.
- При изменении зависимостей (requirements.txt) нужен полный деплой с пересборкой.
