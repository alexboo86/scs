# ⚡ Быстрое обновление на сервере

## Самый простой способ (рекомендуется)

### 1️⃣ Обновить один файл:
```powershell
.\quick-update.ps1 backend\app\api\viewer.py
```

### 2️⃣ Обновить все файлы проекта:
```powershell
.\quick-update.ps1
```

### 3️⃣ Только перезапустить (без обновления):
```powershell
.\restart-backend.bat
```

---

## 🎯 Примеры использования

### Изменили Python файл:
```powershell
.\quick-update.ps1 backend\app\api\documents.py
```

### Изменили HTML шаблон:
```powershell
.\quick-update.ps1 frontend\templates\viewer.html
```

### Изменили несколько файлов - обновить все:
```powershell
.\quick-update.ps1
```

---

## 📋 Что делает скрипт

1. ✅ Копирует файлы на сервер
2. ✅ Перезапускает backend контейнер
3. ✅ Изменения применяются сразу

---

## 🔧 Настройка алиасов (опционально)

Для еще более быстрого доступа:

```powershell
# Настроить один раз
.\setup-powershell-aliases.ps1

# Перезагрузить PowerShell
. $PROFILE

# Теперь можно использовать короткие команды:
update backend\app\api\viewer.py
restart
logs
```

---

## ⚠️ Важно

- **Hot-reload**: Если нужно обновить без перезапуска (если volume mount работает), используйте:
  ```powershell
  .\quick-update.ps1 backend\app\api\viewer.py -NoRestart
  ```

- **Полный деплой**: Если изменили `requirements.txt` или `Dockerfile`, нужен полный деплой:
  ```powershell
  .\deploy.ps1
  ```

- **Проверка логов**: После обновления проверьте что все работает:
  ```powershell
  ssh root@89.110.111.184 "cd ~/projects/secure-content-service && docker-compose logs -f backend"
  ```

---

## 📝 Быстрая справка

| Команда | Описание |
|---------|----------|
| `.\quick-update.ps1 файл.py` | Обновить файл и перезапустить |
| `.\quick-update.ps1` | Обновить все файлы и перезапустить |
| `.\quick-update.ps1 файл.py -NoRestart` | Обновить без перезапуска |
| `.\restart-backend.bat` | Только перезапустить |
| `.\deploy.ps1` | Полный деплой с пересборкой |
