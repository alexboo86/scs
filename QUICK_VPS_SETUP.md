# Быстрая настройка VPS - Пошаговая инструкция

## Шаг 1: Подключение к VPS

```bash
ssh root@YOUR_VPS_IP
```

Или если используете другого пользователя:
```bash
ssh username@YOUR_VPS_IP
```

---

## Шаг 2: Проверка системы

```bash
# Проверить версию ОС
cat /etc/os-release

# Проверить доступное место
df -h

# Проверить память
free -h
```

---

## Шаг 3: Обновление системы

```bash
sudo apt update
sudo apt upgrade -y
```

---

## Шаг 4: Создание пользователя для разработки

```bash
# Создать пользователя
adduser developer

# Добавить в sudo
usermod -aG sudo developer

# Переключиться на пользователя
su - developer
```

---

## Шаг 5: Установка Docker

```bash
# Установить Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавить пользователя в группу docker
sudo usermod -aG docker $USER

# Проверить установку
docker --version
docker-compose --version
```

**Важно:** После этого нужно выйти и зайти заново:
```bash
exit
ssh developer@YOUR_VPS_IP
```

---

## Шаг 6: Установка Git и других инструментов

```bash
sudo apt install -y git curl wget vim nano htop
git --version
```

---

## Шаг 7: Настройка SSH ключей (опционально, но рекомендуется)

**На вашем компьютере (Windows PowerShell):**

```powershell
# Проверить, есть ли SSH ключ
ls ~/.ssh

# Если нет, создать новый
ssh-keygen -t ed25519 -C "your_email@example.com"

# Скопировать публичный ключ
cat ~/.ssh/id_ed25519.pub
```

**На VPS:**

```bash
# Создать директорию для ключей
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавить ваш публичный ключ (вставьте вывод cat из предыдущей команды)
nano ~/.ssh/authorized_keys
# Вставьте ваш публичный ключ, сохраните (Ctrl+O, Enter, Ctrl+X)

# Установить правильные права
chmod 600 ~/.ssh/authorized_keys
```

---

## Шаг 8: Настройка файрвола

```bash
# Разрешить SSH (важно сделать ПЕРВЫМ!)
sudo ufw allow 22/tcp

# Разрешить порты для приложения
sudo ufw allow 8000/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Включить файрвол
sudo ufw enable

# Проверить статус
sudo ufw status
```

---

## Шаг 9: Перенос проекта

### Вариант A: Через Git (если проект на GitHub)

```bash
cd ~
git clone https://github.com/YOUR_USERNAME/secure-content-service.git
cd secure-content-service
```

### Вариант B: Через SCP (с вашего компьютера)

**На вашем компьютере (PowerShell):**

```powershell
scp -r D:\WORK\secure-content-service developer@YOUR_VPS_IP:~/projects/
```

**На VPS:**

```bash
cd ~/projects/secure-content-service
```

---

## Шаг 10: Настройка проекта

```bash
# Создать .env файл
cp env.example .env
nano .env
```

**Минимальная конфигурация .env:**
```env
DATABASE_URL=sqlite:///./secure_content.db
SECRET_KEY=your-very-secret-key-change-this
ALLOWED_ORIGINS=http://YOUR_VPS_IP:8000,https://yourdomain.com
ALLOWED_EMBED_DOMAINS=your-tilda-domain.com
```

Сохраните файл: `Ctrl+O`, `Enter`, `Ctrl+X`

---

## Шаг 11: Запуск проекта

```bash
# Собрать образы
docker-compose build

# Запустить контейнеры
docker-compose up -d

# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f backend
```

---

## Шаг 12: Проверка работы

Откройте в браузере:
```
http://YOUR_VPS_IP:8000/docs
```

Должна открыться документация API.

---

## Готово! 🎉

Теперь ваш проект работает на VPS.

**Полезные команды:**
```bash
# Логи
docker-compose logs -f backend

# Перезапуск
docker-compose restart backend

# Остановка
docker-compose stop

# Запуск
docker-compose start
```

---

## Следующие шаги (опционально)

1. Настроить домен и SSL (см. VPS_DEPLOYMENT.md)
2. Настроить автоматические бэкапы
3. Настроить мониторинг
