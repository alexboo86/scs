# Руководство по переносу проекта на ноутбук

## Вариант 1: Через Git (Рекомендуется) ⭐

### На текущем компьютере:

1. **Настройте Git (если еще не настроен):**
   ```bash
   git config --global user.email "ваш@email.com"
   git config --global user.name "Ваше Имя"
   ```

2. **Создайте коммит:**
   ```bash
   cd D:\WORK\secure-content-service
   git add .
   git commit -m "Initial commit: secure content service"
   ```

3. **Создайте репозиторий на GitHub/GitLab/Bitbucket:**
   - Зайдите на github.com (или gitlab.com)
   - Создайте новый репозиторий (например, `secure-content-service`)
   - НЕ добавляйте README, .gitignore или лицензию (они уже есть)

4. **Подключите удаленный репозиторий и отправьте код:**
   ```bash
   git remote add origin https://github.com/ваш-username/secure-content-service.git
   git branch -M main
   git push -u origin main
   ```

### На ноутбуке:

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/ваш-username/secure-content-service.git
   cd secure-content-service
   ```

2. **Восстановите зависимости:**
   ```bash
   docker-compose up -d
   ```

---

## Вариант 2: Через USB/Внешний диск 💾

### На текущем компьютере:

1. **Скопируйте всю папку проекта:**
   ```
   D:\WORK\secure-content-service → USB:\secure-content-service
   ```

2. **Скопируйте чаты Cursor (опционально):**
   ```
   C:\Users\alex\.cursor\projects\d-WORK\agent-transcripts\ → USB:\cursor-chats\
   ```

### На ноутбуке:

1. **Скопируйте проект:**
   ```
   USB:\secure-content-service → C:\WORK\secure-content-service (или любая другая папка)
   ```

2. **Восстановите зависимости:**
   ```bash
   cd C:\WORK\secure-content-service
   docker-compose up -d
   ```

---

## Вариант 3: Через облачное хранилище ☁️

### Google Drive / OneDrive / Dropbox:

1. **Создайте папку в облаке:**
   ```
   Облако:\Projects\secure-content-service
   ```

2. **Скопируйте проект:**
   ```
   D:\WORK\secure-content-service → Облако:\Projects\secure-content-service
   ```

3. **На ноутбуке синхронизируйте папку из облака**

---

## Перенос чатов Cursor 💬

### Расположение чатов:
```
C:\Users\alex\.cursor\projects\d-WORK\agent-transcripts\
```

### Метод 1: Копирование папки
```bash
# Скопируйте всю папку agent-transcripts на USB/облако
# На ноутбуке скопируйте в аналогичное место:
C:\Users\ваше-имя\.cursor\projects\d-WORK\agent-transcripts\
```

### Метод 2: Экспорт через Cursor
1. Откройте Cursor
2. Найдите историю чатов в интерфейсе
3. Экспортируйте нужные чаты (если есть такая функция)

### Метод 3: Ручное копирование файлов
- Найдите файлы `.txt` в папке `agent-transcripts`
- Скопируйте нужные файлы на ноутбук

---

## Что нужно перенести обязательно ✅

1. ✅ **Весь код проекта** (`D:\WORK\secure-content-service`)
2. ✅ **Файл `.env`** (если есть, с секретными ключами)
3. ✅ **Docker volumes** (если есть важные данные в БД)
4. ✅ **Чаты Cursor** (опционально, для истории)

## Что НЕ нужно перенести ❌

1. ❌ `__pycache__/` (автоматически пересоздастся)
2. ❌ `venv/` или `env/` (переустановите зависимости)
3. ❌ `.git/` (если используете Git, клонируйте заново)
4. ❌ Docker образы (скачаются автоматически при `docker-compose up`)

---

## Быстрая проверка после переноса 🔍

1. **Проверьте структуру проекта:**
   ```bash
   ls -la secure-content-service/
   ```

2. **Проверьте Docker:**
   ```bash
   docker-compose ps
   ```

3. **Проверьте логи:**
   ```bash
   docker-compose logs backend
   ```

4. **Проверьте работу:**
   - Откройте `http://localhost:8000/docs` в браузере
   - Проверьте viewer: `http://localhost:8000/api/viewer/embed?document_token=...`

---

## Рекомендации 💡

1. **Используйте Git** - это лучший способ для версионирования и переноса кода
2. **Создайте резервную копию** перед переносом
3. **Документируйте изменения** в README.md
4. **Используйте `.env` файлы** для конфигурации (не коммитьте их в Git!)

---

## Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log

# Создать новую ветку
git checkout -b feature/new-feature

# Отправить изменения
git push

# Получить изменения
git pull
```
